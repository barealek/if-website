services:
  nginx:
    build:
      context: .
    ports:
      - "8080:80"
    volumes:
      - ./php/:/var/www/html/
      - ./logs/nginx:/var/log/nginx/
  php:
    image: php:fpm-alpine
    volumes:
      - ./php/:/var/www/html/
      - ./logs/php.log:/var/log/fpm-php.www.log
